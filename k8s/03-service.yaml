apiVersion: v1
kind: Service
metadata:
  name: cgi-devops-practice-service
  namespace: cgi-devops-practice
spec:
  type: ClusterIP
  selector:
    app: cgi-devops-practice-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
