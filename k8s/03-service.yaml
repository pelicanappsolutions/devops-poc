apiVersion: v1
kind: Service
metadata:
  name: devops-poc-service
  namespace: devops-poc
spec:
  type: ClusterIP
  selector:
    app: devops-poc-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
