services:
  app:
    build: .
    image: cgi-devops-practice-app:local
    container_name: cgi-devops-practice-app
    ports:
      - "3000:3000"
    environment:
      APP_ENV: docker-compose
      APP_VERSION: compose-local
    restart: unless-stopped
